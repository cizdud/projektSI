{% extends 'base.html.twig' %}

{% block title %}
    Edit Event {{ event.id|default('') }}
{% endblock %}

{% block body %}
    <h1>Edit Event {{ event.id|default('') }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="Save" class="btn btn-primary"/>
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ path('event_index') }}" class="btn btn-link">
            Back to list
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
